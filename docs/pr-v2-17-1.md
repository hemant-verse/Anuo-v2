# PR-V2-17.1 — Frontend interaction and lint cleanup

## Scope
- Refactor AuthProvider initialization/event refresh so React hook lint does not flag a synchronous refresh callback.
- Make marketplace search explicitly labelled and use `type="search"`.
- Replace the admin listing inspection clickable `<div>` with a keyboard-accessible button.
- Add accessible labels/focus treatment to icon-only admin/sell controls.

## Verification required locally
- `pnpm lint`
- `pnpm build`
- `pnpm test`
- `pnpm test:integration`
- `pnpm e2e`

Browser E2E is intentionally not marked complete by this PR; Playwright must be installed as a dev dependency and exercised against the real application environment before that gate is closed.
