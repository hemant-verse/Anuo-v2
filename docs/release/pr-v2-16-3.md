# PR-V2-16.3 — E2E Contract and Rate-Limit Isolation

## Purpose
Close the two concrete failures found by the PR-V2-16.2 HTTP E2E run without weakening production security.

## Fixes
- Corrected the E2E product-detail assertion to read the canonical `{ success, data: { product } }` response envelope.
- Added deterministic E2E request identities so login/refresh rate limits do not leak state between independent test scenarios or previous runs.
- E2E fixture emails remain within the canonical `@indoreinstitute.com` contract.
- Each E2E run still resets only the configured test database.

## Security decision
Production rate-limit policies are unchanged. The test suite is isolated by deterministic test identities rather than disabling or bypassing rate limiting.

## Verification target
- Unit: 10/10 or current suite total, 0 failures
- Integration: 7/7 or current suite total, 0 skipped
- HTTP E2E: 3/3, 0 failures
