# PR-V2-06 — Product API Completion and Consumer Migration

## Goal
Move active marketplace consumers to canonical V2 Product/Favorites resources without deleting compatibility adapters prematurely.

## Completed
- Canonical Favorites service and resource routes.
- Marketplace feed migrated to `GET /api/products`.
- Marketplace favorites migrated to `/api/favorites`.
- Product detail migrated to `GET /api/products/:id` and canonical Product fields.
- My Listings migrated to `GET /api/products?mine=true`, `PATCH /api/products/:id`, and `DELETE /api/products/:id`.
- Categories migrated to canonical Product fields.
- Admin pending-listing server query migrated to canonical Product fields.
- Phantom `location` UI assumptions removed from migrated marketplace views.
- Legacy product/favorites routes retained as explicit adapters.
- Canonical Product service remains the only persistence owner for product mutations.

## Verification
- JavaScript syntax check: PASS.
- Unit tests: 5/5 PASS.
- Full Next.js build/lint remains environment-blocked until project dependencies are installed.
